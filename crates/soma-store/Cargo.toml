[package]
name = "soma-store"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.40.2", features = ["bundled"] }
serde_json = "1.0.151"
soma-core = { version = "0.1.0", path = "../soma-core" }
thiserror = "2.0.20"

[dev-dependencies]
tempfile = "3.27.0"
